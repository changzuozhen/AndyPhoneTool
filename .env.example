# 复制为 .env.local（仅放 Expo 可读的安全变量）
#
# Expo SDK 57+ 会拒绝 .env 中的 NPM_CONFIG_* 等危险变量。
# npm 镜像请使用项目根目录 .npmrc，二进制包镜像见 scripts/npm-binary-mirrors.env.example

# 关闭 Expo 遥测
EXPO_NO_TELEMETRY=1

# Metro 使用工作区根目录解析
EXPO_USE_METRO_WORKSPACE_ROOT=1
